<?xml version="1.0"?>
<robot xmlns:xacro="http://www.ros.org/wiki/xacro" name="origin_macro">
    <xacro:macro name="calibrated_origin" params="name x:=0 y:=0 z:=0 roll:=0 pitch:=0 yaw:=0">
            <origin xyz="${x} ${y} ${z}" rpy="${roll} ${pitch} ${yaw}" />
    </xacro:macro>
</robot>
